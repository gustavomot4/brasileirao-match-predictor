---
tags: [scb, decisoes, adr]
status: vivo
tipo: decisoes
data: 2026-07-14
---

# Decisões técnicas do SCB (ADRs)
Registro curto de *por que* cada escolha. **Append-only** — decisão nova = linha nova; decisão revertida = linha nova citando a antiga (nunca editar a antiga). Numeração própria do SCB (as D-NN do SCM são citadas como "D-NN SCM").

| # | Data | Decisão | Por quê |
|---|---|---|---|
| D-01 | 2026-07-14 | **Herdar as 7 regras de negócio do SCM inalteradas** (R$0, local/offline no cálculo, probabilidades, registro imutável, não inventar dados, sem ML, portão de backtest) | são agnósticas ao campeonato (viabilidade 2026-06-28); mudá-las = decisão do Gustavo, não de agente |
| D-02 | 2026-07-14 | **Port do `scm_analytics`** (fork → `scb_analytics`, pacote `scb/`), não reescrita | ~70% entra sem mexer; reescrever re-introduz bugs já pagos (D-22/34/80/82/83/85 SCM). Decisão do Gustavo nesta data |
| D-03 | 2026-07-14 | **Multi-liga desde o dia 1**: ingest/config parametrizados (`leagues.json`); **E0 valida o port, BRA é a entrega** | isola "bug de port" de "dificuldade da liga"; football-data entrega tudo no mesmo formato; custo marginal baixo. Decisão do Gustavo nesta data |
| D-04 | 2026-07-14 | **football-data.co.uk = fonte primária** (BRA extra + E0 main; `notes.txt` versionado); martj42 sai | única fonte grátis com resultados + odds de abertura E fechamento; resolve a lacuna nº 1 do SCM (CLV manual). Verificada online em 2026-07-14 |
| D-05 | 2026-07-14 | **A lista-morta do SCM NÃO transfere**: todo termo rejeitado lá re-passa pelo portão aqui (e vice-versa: os adotados também) | o dado e o contexto mudaram (ex.: descanso era ~0 em seleção e é candidato real em clube; altitude era real na Copa e é ~0 no Brasil) |
| D-06 | 2026-07-14 | **Termos de Copa ficam OFF/fora do pipeline**: altitude, confederação, knockout/ε-pênaltis, bracket, tempo do gol | ver contrato §2; código permanece no repo como candidato OFF documentado (custo zero, reativável) |
| D-07 | 2026-07-14 | **Curva de empate C1 reconstruída POR LIGA** (tabela `P_E(\|dr\|)` empírica da própria liga, PIT, congelada com a versão) | regime de empate de liga ≠ seleções; usar a curva do martj42 seria erro sistemático de P(E). Família da D-26 SCM |
| D-08 | 2026-07-14 | **Validação = walk-forward por temporada** (rolling origin), IC bootstrap pareado B=10k seed=12345; **4 réguas**: uniforme, taxa-base da liga, Elo-puro, mercado de-vigged (abertura e fechamento) | mais honesto p/ liga (churn entre temporadas) que o corte fixo <2015/≥2015 do SCM; taxa-base é a régua que liga equilibrada exige; mercado agora tem série histórica grátis |
| D-09 | 2026-07-14 | **Teto do peso de mercado no ensemble mantido em 0,20** (D-08 SCM) | regra de negócio herdada; ver Q-01 abaixo |
| D-10 | 2026-07-14 | **Alvo de operação: temporada 2026 em andamento** — registro prospectivo começa na M5, sempre DEPOIS do baseline validado (M4) | decisão do Gustavo nesta data; "baseline primeiro" preservado |
| D-11 | 2026-07-15 | **[[PLANO]] v1.0 e contrato [[MODELO-MATEMATICO\|SCB v1.0]] CONGELADOS** (aprovação do Gustavo) — portão da Fase 1 passado | base estável para M1+; mudança de rumo = D-NN novo; mudança de fórmula = bump de `MODEL_VERSION`; nunca replanejar do zero |
| D-12 | 2026-07-15 | **Pasta SCB = vault Obsidian** (wikilinks, frontmatter, [[Indice]] como nota-casa, `.obsidian/` mínimo versionável, workspace no `.gitignore`) | mesma prática do SCM (D-09 SCM: vault in-place); BACKLOG usa o plugin Kanban; navegação e grafo funcionam |
| D-13 | 2026-07-15 | **BRA.csv tem SÓ odds de FECHAMENTO** [medido no header real: PSC/MaxC/AvgC/BFEC/B365C; sem abertura, sem OU/AH] → CLV e régua de mercado do backtest automáticos; perna de mercado em produção pré-jogo = captura manual opcional (`odds_close` herdado); backtest usa fechamento rotulado como teto; E0 (abertura+fechamento [medido]) mede o gap | correção de fato sobre a suposição do plano ("abertura e fechamento"); registrada como nota M1 no contrato §3.5 — fórmula do ensemble inalterada, sem bump. Detalhe: [[POC-M1-dados (2026-07-15)]] |
| D-14 | 2026-07-15 | **Q-02 decidida: SEM Kaggle por ora.** Elo do BRA nasce do próprio BRA.csv (2012+ [medido]) com **burn-in interno de 2 temporadas** (2012–13 fora da avaliação); σ_R provisório cobre a maturação | 2ª fonte = risco de alias/duplicata + sem odds + qualidade não auditada, para ganho marginal (~14 temporadas bastam para o baseline). Reabre se o backtest mostrar Elo imaturo nas primeiras temporadas de teste |
| D-15 | 2026-07-15 | **Layout do código: `scb_analytics/` na raiz do vault** (pacote `scb/`, `dados/`, `scripts/`) — snapshot e dados curados **versionados** (`notes.txt`, `leagues.json`, CSVs do football-data); `.gitignore` só barra `*.sqlite`/`*.png` | espelho do D-10/D-78 SCM (vault+código juntos; clone roda offline de primeira). `leagues.json` criado com campos [confirmar] explícitos (Q-03) |
| D-16 | 2026-07-15 | **M1 FECHADA (portão passou)** + regra derivada: **coluna de mercado = cadeia de fallback por temporada (PSC → AvgC → B365C)** | run completo [medido]: BRA 5.497 jogos 2012–2026, empate 26,8%, gols 2,40, 0 dup/0 alias; E0 12.704 jogos 93/94+, fechamento completo desde 19/20. Única surpresa: **Pinnacle closing = 88% em 2025 e 0% em 2026** (morte do Pinnacle atinge a temporada-alvo) → sem a cadeia de fallback, a régua de mercado e o CLV de 2026 ficariam vazios. QA-01/02/03 do parser corrigidos (7/7 harness) e herdados pelo ingest da M2 como fixtures. Detalhe: [[POC-M1-dados (2026-07-15)]] |

| D-17 | 2026-07-16 | **Baseline `scb-v0.1` VALIDADO pelo portão da M4** (walk-forward, curva por fold, B=10k): BRA Brier 0,6146 bate uniforme/taxa-base/Elo-puro com IC>0; E0 0,5899 idem; mercado à frente ~2pp (teto declarado) | congela o baseline (pendente run oficial); evolução só via fila do portão. Achado p/ M6: banda sub-cobre nos extremos (padrão D-30 SCM). Detalhe: [[Backtest baseline (2026-07-16)]] |

| D-18 | 2026-07-16 | **Simulador de liga (M5)**: fixtures restantes DERIVADAS do turno-returno (fonte sem calendário futuro); forças ESTÁTICAS na sim (como no SCM); desempate pontos→vitórias→saldo→gols pró→sorteio, com **cartões pulados (lacuna de dado)** e **confronto direto fora da V1** (simplificação declarada) | jogos reais travados (D-83/85 SCM); seed fixa; invariantes testados (Σtítulo=1). Mudar a ordem do desempate = 1 linha no sort (Q-03) |

| D-19 | 2026-07-16 | **M6.1 REJEITADA pelo portão: recalibração ESTÁTICA de H/T_base** (`scb/calibrate.py`, grid 2014–19 → gate 2020–26): candidato H=120/T_base=2,20 piora gols com IC<0 (Δ−0,0065 IC[−0,0101,−0,0031]) e não melhora o 1X2 (IC cruza) | **regime inverteu entre eras** (mando BRA caiu pós-COVID; gols subiram 2,18→2,66) — mesmo padrão D-25/D-40 SCM. Caminho: **C3 janela móvel PIT** (família APROVADA no SCM, D-84) p/ gols, e variante rolling p/ mando como candidato novo. Baseline v0.1 segue como está — o gate protegeu o modelo |

| D-20 | 2026-07-16 | **M6.2 REJEITADA pelo portão: drift PIT do canal de gols (C3)** — `scb/drift.py` (família D-84 SCM): validação BRA over Δ−0,00010 IC[−0,00181,+0,00159] / BTTS +0,00005 IC cruza; E0 idem; kill-switch ok (corr ~0). **E0 também rejeitou a M6.1** (H*=100=atual; T_base do treino regride gols IC[−0,0058,−0,0019]) — completa a D-19 | o ganho do SCM vinha da estrutura por CLASSE (amistoso/major) que liga única não tem; a deriva recente do BRA (25-26) fica atrás da janela. C3 vai à lista-morta COM números (re-propor só com ângulo novo). Baseline v0.1 segue o melhor modelo conhecido |

| D-21 | 2026-07-16 | **M6.3 mando rolling PIT (`scb/mando_rolling.py`): PASSA na E0, rejeitado no BRA** — E0 validação n=6.080: 1X2 **+0,00180 IC[+0,00059,+0,00302]**, gols +0,00066 IC>0, kill-switch −0,004, δ vigente **−36,8 Elo** (queda estrutural do mando pós-COVID); BRA: −0,00087 IC cruza (δ −12, ruidoso) | **1º termo a passar um portão no SCB.** Adoção é DECISÃO SEPARADA do Gustavo (Q-04): wiring por liga + bump `scb-v0.2-mando-e0` + re-run M4. Flag OFF até lá. Família = ângulo novo da D-19 (rolling em vez de estático) — o método funcionando |

| D-22 | 2026-07-16 | **M6.4 REJEITADA: descanso diferencial INTRA-LIGA (C1)** — `scb/descanso.py`: BRA Δ1X2 −0,00019 IC[−0,00041,+0,00003]; E0 −0,00003 IC cruza; kill-switch ok | causa visível no STEP 0: \|diff\| médio 0,76d/0,67d — rodadas são simétricas DENTRO da liga; a congestão real (Libertadores/Copa do Brasil/qua-dom) não está na fonte (lacuna declarada em [[DADOS]]). **Re-testar SÓ com calendário externo grátis** (= ângulo novo exigido pela D-05). C1 à lista-morta como-testado |

| D-23 | 2026-07-16 | **M6.5 Dixon-Coles (C2): PASSA na E0 (canal do empate), rejeitado no BRA** — E0: ρ*=−0,05 (sinal clássico), empate-Poisson **+0,00039 IC[+0,00017,+0,00062]**, over/BTTS neutros; BRA: ρ* do treino veio **+0,05** e regrediu na validação (−0,00076 IC<0) — dependência de placar baixo instável entre eras | 2º termo aprovado na E0 (com o mando rolling, D-21); BRA segue baseline limpo. Adoção exige **2º gate** (re-blend do 1X2 do ensemble com a matriz DC) — Q-05. `scb/dixon_coles.py` + 3 testes |

| D-24 | 2026-07-16 | **M6.6 regressão de temporada (C5): PASSA NO BRA (1º termo aprovado na liga-produto!), rejeitada na E0** — BRA: ρ*=0,30 (calib), validação Δ1X2 **+0,00216 IC[+0,00028,+0,00418]**, gols +0,00107 IC>0; E0: IC cruza (elencos estáveis — churn é fenômeno brasileiro) | `scb/season_rho.py` (gate com rebuild por candidato; hook já testado na M3.1). Caveats honestos: IC inferior perto de zero (passa formal, magnitude modesta) e ρ*=0,30 na borda da grade (checar 0,4 na adoção). **Q-06**: adoção = SEASON_RHO por liga + bump `scb-v0.2` + rebuild + re-run M4 |

| D-25 | 2026-07-16 | **Q-06 EXECUTADA: regressão de temporada ADOTADA NO BRA (ρ=0,30) → `scb-v0.2-rho-bra`** — 1ª adoção do produto. ρ=0,40 checado e descartado (calib pior; valid IC cruza). Wiring POR LIGA (`config.SEASON_RHO` dict + `season_rho_for`; `EloParams.season_rho=None` → config); +1 teste (BRA regride, E0 não). **M4 re-rodado: BRA 0,6146→0,6131, ECE 0,0288→0,0224, gap p/ mercado −0,0195→−0,0180; E0 IDÊNTICA dígito a dígito** (isolamento provado). Sim 2026: Palmeiras 78,8→79,6%, Fla 20,7→19,6% (transparência antes/depois) | rebuild OBRIGATÓRIO na máquina do Gustavo; Q-04/Q-05 (E0) seguem abertas |

| D-26 | 2026-07-16 | **Q-04 EXECUTADA: mando rolling ADOTADO na E0 → `scb-v0.3-mando-e0`** — wiring POR LIGA no `features_pit` (δ PIT no dr_adj; produção via `delta_today` no predict_match; W=5a) | oficial M4 v0.3: **E0 0,5899→0,5894, ECE 0,0365→0,0290, gap mercado −0,0176→−0,0156; BRA idêntico (0,6131)**. δ embutido medido: E0 −41,5 Elo (2023+), BRA 0,0. Fix de qualidade: default `min_n` congelava no def → `None`+runtime |
| D-27 | 2026-07-16 | **Q-05: DC na E0 REJEITADO no 2º gate (re-blend do 1X2)** — Δ+0,00005 IC[−0,00015,+0,00025]; guarda ok | o ganho do canal do empate (D-23) DILUI no ensemble (perna Poisson=0,56; a curva empírica do Elo já captura a estrutura) — eco exato do D-39 SCM. Capacidade `dc_rho` fica no predictor, testada e OFF |
| D-28 | 2026-07-16 | **Banda/σ_dr MEDIDA (era de validação): σ×1,3 → E0 8/10 faixas (era 6/10; largura +29%); ×1,6 → 9/10 (+57%); BRA neutro** | adoção exige separar BANDA do PONTO no predictor (2ª propagação com σ escalado — senão Jensen mexe no Brier). **Q-07**: implementar na M7/próxima evolução, por liga (E0 1,3; BRA 1,0) |

| D-29 | 2026-07-16 | **M7 web entregue — estilo EA FC por design próprio** (dark + tipografia condensada + cards + trilho de progresso), Flask local, 3 telas (prever/tabela/prospectivo) + launcher `.bat`. **Escudos: SVG gerado com as CORES reais dos clubes** (cores não são protegíveis; escudo oficial é marca) + **override local** `static/logos/<slug>.png` (uso pessoal, decisão do usuário). Fonte via CDN com fallback de sistema (offline funciona; regra 2 intocada — cálculo não lê rede) | sem Node/framework pesado (TECH_STACK herdado); `scb/badges.py` testável sem Flask (5 testes; 71 no total); import do Flask é lazy; Flask não roda no sandbox → teste no navegador é do Gustavo (precedente SCM) |

| D-30 | 2026-07-17 | **Feedback da web executado**: QA-04 contraste dos chips (sólido+texto escuro); Imprimir/PDF + Copiar resumo (CSS de impressão + clipboard) em confronto/tabela; escudos PNG via script do usuário (saga QA-05..QA-10: fim = `--investigar` revelou /CBF/ e nomes oficiais → matching por subconjunto; cache do navegador vencido com `?v=2`) | Premier 100% automática; BRA 4 automáticos + lista manual `dados/escudos-pendentes.md` (fontes públicas esgotadas — declarado) |
| D-31 | 2026-07-17 | **Registro da rodada em 1 clique**: `dados/fixtures.csv` (calendário colado 1x/temporada — a fonte não publica futuro; scraping fura ToS/R$0) + `registrar.auto(dias)` na web ("Registrar rodada") e CLI (`registrar auto`, agendável no Task Scheduler) + **settle acha jogo ADIADO** (par ordenado é único por temporada no turno-returno → casamento seguro) | operação sem terminal e automatizável; imutabilidade preservada (idempotente); 2 testes (73 no total) |
| D-32 | 2026-07-17 | **REJEITADO pelo portão: proxy de xG por CHUTES NO GOL (SoT)** — `scb/sot_edge.py` (δ = clip(θ·edge, ±60) somado ao `dr`; edge = SoT-diff rolling PIT mandante − visitante; K,θ da era de calibração). **E0 validação 2014-2025, n=4560, K\*=5 θ\*=10: 1X2 Δ−0,00050 IC[−0,00184,+0,00083]** (cruza zero, não passa) · gols Δ−0,00091 IC[−0,00137,−0,00047] (REGRIDE) · ECE 0,0329→0,0454 (REGRIDE); kill-switch corr(δ,dr) +0,698 (ok — independente, não é a causa). BRA não se aplica (a fonte não tem stats de jogo) | o gate-0 mostrou sinal no SALDO (corr parcial 0,11 além do Elo), mas como termo aditivo ao `dr` esse sinal marginal DILUI no ensemble e desloca λ piorando over/BTTS e a calibração — eco direto do Dixon-Coles (D-27). Vai à lista-morta COM números. **2º ângulo TAMBÉM testado e rejeitado** (canal 'gd': δ desloca SÓ a margem da Poisson via `predictor.gd_extra`, `dr`/Elo-direto intactos): 1X2 Δ+0,00011 IC[−0,00021,+0,00043] (cruza zero) · gols Δ−0,00049 IC[−0,00080,−0,00017] (REGRIDE — super-afia o placar e piora o BTTS) · ECE ok. **Conclusão**: o sinal do SoT (parcial 0,11) é real mas fraco e ENTRELAÇADO demais com o Elo (corr 0,70) p/ ajudar o ensemble já calibrado, em qualquer canal de margem. **3º ângulo — SoT-TOTAL → T_m** (feature DISTINTA da diferencial; canal 'tm' via `predictor.tm_extra`, baseline móvel PIT drift-robusta): **MELHORA os gols (over/BTTS) Δ+0,00133 IC[+0,00057,+0,00210]** (IC>0!) + ECE 0,033→0,031, INDEPENDENTE do Elo (corr 0,07) — mas trips a guarda de não-regressão do 1X2 por um fio (Δ−0,00007, IC superior ≈ 0). **Não adotado no λ compartilhado** (perturba minimamente o 1X2, saída primária), porém é o 1º sinal de SoT REALMENTE ÚTIL — para os MERCADOS DE GOLS. **Caminho recomendado (candidato futuro, D-NN próprio)**: SoT-total DESACOPLADO — aplicar `tm_extra` SÓ nas saídas over2.5/BTTS, não no 1X2 — o que zera a regressão da saída primária. Pontos de injeção `gd_extra`/`tm_extra` no predictor ficam dormentes (default 0 — `MODEL_VERSION` inalterada). Base `match_stats` (E0 2000-2025) segue p/ exibição |
| D-33 | 2026-07-17 | **PASSA no portão: SoT-total DESACOPLADO no canal de gols** (1º termo de SoT adotável) — `scb/sot_edge.py` canal `tm_goals`: over2.5/BTTS recalculados com o TOTAL deslocado por `tm_extra` (`over_btts_tm`), enquanto **1X2/placar/ECE ficam INTOCADOS por construção** (λ base). E0 validação 2014-2025, n=4560, K\*=10 θ\*=0,08 gols: **gols Δ+0,00133 IC[+0,00058,+0,00210]** (IC>0 ✅) · 1X2 Δ=0 exato · ECE inalterado · kill-switch corr −0,01 (independente do Elo) | resolve a fronteira da D-32: o desacoplamento zera a regressão de 1X2 e entrega o ganho de gols limpo. Só p/ E0 (BRA sem stats → `tm_extra`=0, inócuo). **Adoção = decisão separada (M6.x, próximo passo)**: wiring do `tm_extra` PIT (baseline móvel drift-robusta) em `predict_now`/`predictor.run` no canal de gols + flag `SOT_GOALS` por liga + bump `scb-v0.4-sot-goals-e0` + rebuild + re-run do backtest confirmando o novo baseline de over/BTTS. Teste PIT do módulo cobre `total_series`/`_roll_pit`/`over_btts_tm` |
| D-34 | 2026-07-17 | **2ª fonte de dados DECLARADA: API-Futebol (api-futebol.com.br) p/ stats do Brasileirão** — o football-data não traz chutes/escanteios/cartões do BRA; a API-Futebol traz (CONFIRMADO no plano grátis via chamada real: `estatisticas.finalizacao.no_gol` = chutes NO GOL/SoT, `.total` = chutes, `escanteios`, `faltas`, `posse_de_bola` + `cartoes` contados por array). `scripts/baixar_stats_bra.py` (VOCÊ roda; chave `live_...` via env `APIFUTEBOL_KEY`, auth Bearer; resumível, respeita cota) → snapshot `dados/bra_stats.csv` → `ingest.load_bra_stats` casa em `match_stats` por (par de times + data ±2d, alias de nomes), OFFLINE. Parser puro `parse_apifutebol_stats` + 4 testes | regras mantidas: **R$0** (grátis), **download à parte** (nada lê a internet no cálculo — snapshot em disco), **não inventa** (sem casamento = pula). 1º uso = painel de EXIBIÇÃO (Prever Confronto acende p/ o BRA sozinho com o dado). Termo do MODELO (SoT-gols no BRA via `no_gol`, à la D-33) vira candidato FUTURO: precisa de histórico + passar no próprio portão (termos da E0 não replicam no BRA — vide mando/DC) |
| D-35 | 2026-07-17 | **D-33 ADOTADO → `scb-v0.4-sot-goals-e0`** (SoT-total desacoplado no canal de gols da E0): wiring do `tm_extra` PIT em `predict_now`/`predictor.run`/backtest, flag `SOT_GOALS` por liga (E0 on; resto off → inócuo), bump de `MODEL_VERSION`, rebuild na máquina do Gustavo | fecha a linha D-33. Over/BTTS da E0 melhoram (Δ+0,00133 IC[+0,00058,+0,00210]); **1X2/placar/ECE intocados por construção** (λ base). Rebuild confirmado pelo Gustavo: **85 testes verdes, backtest E0 Brier 0,5894**. BRA sem stats → `tm_extra`=0 (idêntico) |
| D-36 | 2026-07-21 | **API-Futebol vira 2ª fonte de RESULTADOS do BRA (não só stats)** — parser captura o placar; `load_bra_stats` INSERE em `matches` os jogos que o football-data ainda não publicou (mesma lógica do `resultados_extra`/D-80 SCM, com guarda ±2d/D-82). `match_stats` +8 colunas (posse/passes/desarmes/defesas) via migração `_migrate` idempotente (`ALTER TABLE` — `CREATE IF NOT EXISTS` não altera tabela já existente). Fetcher endurecido: captura placar+posse, **self-healing** (reprocessa linha sem placar; reescreve dedup), **trata 429** (grava sempre via `try/finally`; para com elegância) e busca os **recentes primeiro** | o "Liquidar resultados" não liquidava nada — **não por bug, mas por falta de dado**: o football-data só tinha o BRA até 1º/jun; a API-Futebol já tinha os placares de julho. Regras mantidas (R$0; download à parte; **não inventa**: sem placar não insere) |
| D-37 | 2026-07-21 | **Escudos OFICIAIS do BRA via API-Futebol** — `scripts/baixar_escudos_bra.py`: 20 URLs de escudo (CDN público) MAPEADAS por clube (100% casaram pelo alias do ingest), baixa sem chave/sem cota → `static/logos/<slug>.svg`. Resolve os 16 clubes pendentes → `escudos-pendentes.md` OBSOLETO. Coventry adicionado ao alias do `baixar_escudos.py` (E0) | o matcher por semelhança do GitHub (D-30) chutava e deixava 16 brasileiros sem escudo; a própria fonte de dados (D-34) já dá o escudo CERTO de cada clube. O CDN é público → não gasta a cota (baixa) da API |
| D-38 | 2026-07-21 | **Calendário completo do BRA + tela Classificação real** — `fixtures.csv` reescrito com 193 jogos (rodadas 19-38, extraídos do calendário da API) mantendo a Premier intacta; nova aba **Classificação** na Tabela = classificação REAL dos jogos disputados (a partir de `matches`), ao lado da projeção Monte Carlo | antes só a rodada 19 estava no `fixtures.csv` (o "Registrar rodada" não enxergava o resto da temporada); a classificação real fecha o par com a simulada (projeção × realidade) |
| D-39 | 2026-07-21 | **Painel de Calibração** — nova aba que expõe se o modelo merece confiança: Brier + ECE + curva de confiabilidade das previsões walk-forward (tabela `predictions`, Elo pré-jogo) **e** comparação com o mercado (odds de fechamento sem margem, `odds_hist`). Medido: E0 ECE 2,3% e fecha **78%** da distância taxa-base→mercado; BRA ECE 1,1% e fecha **50%**. Só leitura (não roda o modelo; com cache) | era a maior lacuna: havia registro e settle, mas nunca se via SE o modelo acerta. Responde "dá pra confiar nas %?" e "chega perto do mercado?" — usa dado que já existia; aumenta o valor de todas as outras telas |
| D-40 | 2026-07-21 | **Artilharia ADICIONADA e depois CORTADA (reversão honesta)** — tela de artilheiros do BRA (1 chamada, snapshot) construída e, no mesmo trabalho, REMOVIDA a pedido do dono: tela, rotas, helper, parser, teste, coleta embutida no fetcher e o CSV | exibição pura — não alimenta o modelo, não muda nenhuma probabilidade. Num sistema de PREVISÃO agrega contexto, não valor preditivo. Lição: **"barato" (1 chamada, dado na mão) ≠ "valioso"**; foi sugestão de agente e o corte foi a decisão certa do dono |
| D-41 | 2026-07-22 | **M7.2 empacotamento + higiene de repo** — zip de entrega `scb-v0.4-sot-goals-e0` (162 arq, 1,4 MB) só com fonte + docs + dados curados (sem `.venv`/`.git`/`__pycache__`/`*.sqlite`/`*.png`/segredo), CONFERIDO aberto: extraído num diretório limpo roda **do zero offline** (90 testes verdes; `ingest` reconstrói BRA 5.499/E0 12.704 só com os CSVs). Higiene: `.gitattributes` (`* text=auto eol=lf`) porque 44 arquivos apareciam "modificados" no git só por CRLF (LF→CRLF do Windows) — `--ignore-all-space` provou **0 dado real**; normalizados de volta p/ LF. README do `scb_analytics` sincronizado M2→M7.1 (90 testes, pipeline, módulos). `*.zip` no `.gitignore` | fecha a **Fase 6** do pipeline — o clone/zip roda offline de primeira (lição D-78 SCM; evita o zip de 3,5 GB com `.venv`). Segredos: chave da API só via `os.environ` (Bearer), nunca no repo. **Portão final (pytest verde na máquina do Gustavo) é do dono** — validado no sandbox (Linux/py3.10). Nota: um `index.lock` órfão criado por varredura em sandbox (sem permissão de unlink) foi removido pelo dono |
| D-42 | 2026-07-22 | **ESPN vira 2ª fonte GRÁTIS (resultados + stats + calendário) e SUBSTITUI a API-Futebol (paga) na operação — botão "⟳ Atualizar rodada" na web** — `scb/espn.py`: parser DETERMINÍSTICO da API pública da ESPN (`site.api.espn.com/.../soccer/bra.1/scoreboard`, sem chave, sem custo). Mapa dos 20 times ancorado no **ID estável** da ESPN → nome do banco; time fora do mapa é PULADO com aviso (não inventa). 1 clique em **Prospectivo → "⟳ Atualizar rodada (ESPN)"** (rota `/api/atualizar`): busca → grava snapshots (`bra_stats.csv`/`fixtures.csv`, sem sobrescrever linha completa) → `load_bra_stats` casa em `matches`/`match_stats` → `settle` liquida. Traz placar, **SoT**, posse, escanteios, faltas, cartões e **HT (derivado do minuto do gol)**; passes/desarmes/defesas ficam em branco (só no boxscore por jogo). +9 testes (`test_espn`: parser contra valores REAIS da rodada 2026-05-31, gravadores, orquestração) → **98 no total** | a API-Futebol virou paga (trial acabou) e seu `?temporada=` NÃO expõe histórico — **falso positivo** que eu tinha cantado no `200` do probe, desmascarado pela data (2026-07-22) no `[CHECK]`. ESPN resolve a operação a R$0. Regras mantidas: **R$0**; **download à parte** (rede só no clique; o cálculo lê o disco); **não inventa** (fonte ESTRUTURADA, não LLM adivinhando). ESPN é API **não-oficial** (zona cinza de ToS, uso pessoal) — mesma categoria da football-data/CDN de escudos. Odds seguem da football-data. **Confirmado ao vivo pelo Gustavo** |
| D-43 | 2026-07-22 | **Tela Prospectivo: filtros + auditoria de acerto** — filtros client-side (Liga · Status liquidado/aberto · Resultado **acerto/erro** · busca por time) que escalam p/ centenas de jogos; selo **✓ ACERTOU / ✗ ERROU** (no palpite mais provável do modelo) + o dado informativo **"modelo dava X% no que aconteceu"** (complementa o Brier, mais honesto que só acerto/erro); resumo que REAGE ao filtro (n · liquidados · acertos% · Brier médio do recorte). Sem back novo (usa campos já na página) | pedido do dono ("separar por acertos/erros/rodadas") com o corte de valor da **D-40**: **Elo-points-ganhos FICOU DE FORA** (é força do time, não qualidade da previsão — enfeite numa tela de auditoria); **rodada** não entrou (o registro guarda data, não rodada — não inventar) |

## Questões abertas (não são decisões ainda)

| # | Questão | Dono | Quando decidir |
|---|---|---|---|
| Q-01 | Com série histórica de odds, o backtest pode medir o peso ótimo do mercado no ensemble. Se der > 0,20: sobe o teto? | Gustavo | M6 (números na mesa: mercado 0,595/0,566 vs modelo 0,615/0,590) |
| Q-04 | ~~mando rolling E0~~ **EXECUTADA (D-26: `scb-v0.3-mando-e0`)** | — | — |
| Q-05 | ~~DC E0~~ **FECHADA: 2º gate rejeitou (D-27)** | — | — |
| Q-07 | **Banda por liga (σ×1,3 na E0)?** — números na D-28; exige 2ª propagação só-banda no predictor | Gustavo | M7 ou próxima evolução |
| Q-08 | **C4 viagem**: exige tabela curada de coordenadas das cidades-sede (todas as ~40 praças históricas do BRA + E0) — tarefa de DADO antes do gate. **C6 H por clube**: pronto-para-rodar com a maquinaria existente (shrinkage forte) | Gustavo (C4: curadoria; C6: pedir o gate) | próxima sessão de evolução |
| Q-06 | ~~Adotar ρ no BRA?~~ **EXECUTADA (D-25: `scb-v0.2-rho-bra`)** | — | — |
| Q-02 | ~~Kaggle?~~ **FECHADA (D-14: não; burn-in interno)** | — | — |
| Q-03 | ~~Desempate CBF~~ **FECHADA (2026-07-16, verificação web):** ordem oficial 2026 = vitórias→saldo→gols pró→confronto direto→menos vermelhos→menos amarelos→sorteio — bate com a implementada (D-18); confronto direto e cartões seguem simplificações declaradas | — | — |
